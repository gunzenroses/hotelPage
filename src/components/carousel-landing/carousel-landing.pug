mixin carousel-landing(backgroundPicture)
  .carousel-landing__frame
    .carousel-landing__list
      for picture in backgroundPicture
        .carousel-landing__element
          img(class="carousel-landing__img" src= picture alt="background pictures")