mixin button-submit({ text = '', theme = '' })
  - 
    const buttonSubmitClass = theme === ''
      ? 'button-submit' 
      : `button-submit button-submit_type_${ theme }`;

  button(
    class = buttonSubmitClass 
    type = 'submit')= buttonLinkText