mixin button-submit({ buttonText = '', theme = '' })
  - 
    var buttonSubmitClass = theme === ''
      ? 'button-submit' 
      : `button-submit button-submit_type_${ theme }`

  button(
    class = buttonSubmitClass 
    type = 'submit')= buttonText