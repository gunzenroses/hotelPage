include ./__menu/header__menu.pug
include ../logo/logo.pug

mixin header({ user="", theme="" })
  if theme == ""
    header.header
      .header__logo
        +logo()
      +header__menu(user)
  else 
    header(class=`header header_in-${theme}`)
      +logo()
      +header__menu(user)