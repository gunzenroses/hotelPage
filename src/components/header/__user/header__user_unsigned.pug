include ../../button/button_type_link.pug

mixin header__user_unsigned(signinText, signinLink, regText, regLink)
  .header__buttons
    .header__signin
      +button_type_link(signinText, signinLink, "signin")
    .header__registration
      +button_type_link(regText, regLink, "registration")